# dependencies
node_modules

# next.js build output
.next
out

# environment — never commit secrets
.env
.env*.local

# misc
.DS_Store
next-env.d.ts
*.tsbuildinfo

# Room snapshots, so a restart doesn't end the night
.bignight/

# Local throwaway Postgres data, if PGlite is ever given a path
.pglite/
